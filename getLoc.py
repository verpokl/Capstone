from gps import *
import time

class GetLocation:
    def getLocation(gps)
    data = gpsd.next()
    if loc['class'] == 'TPV':
        loc = [getattr(loc,'lon'), 'unknown'], getattr(loc, 'lat', 'unknown')]
        return loc
